{
  "network": {
    "servers": [ "5.39.90.146:5000" ],
    "timeout": 15,
    "ssl ca": "/opt/logstash-forwarder.crt"
  },
  "files": [
    {
      "paths": [
        "/var/log/apache2/*.log",
        "/var/log/nginx/*.log"
       ],
      "fields": { "type": "web" }
    },

    {
      "paths": [
        "/var/log/mail.log"
       ],
      "fields": { "type": "mail" }
    }
   ]
}
