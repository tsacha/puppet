input {
  tcp {
    type => syslog
    port => 5544
    codec => "json"
  }
  collectd {}
}
 
output {
  elasticsearch {
    bind_host => "kerbin.trs.io"
    cluster => "logstash"
    port => 9300
  }

  stdout {
    codec => rubydebug
  }
}
