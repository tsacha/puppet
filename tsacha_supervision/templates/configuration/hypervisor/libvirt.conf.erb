object Service "libvirt" {
  import "generic-service"

  host_name = "<%= @hostname %>"
  check_command = "by_ssh"
  vars.by_ssh_command = "/etc/init.d/libvirt-bin status"
  vars.by_ssh_options = "StrictHostKeyChecking=no"
  vars.sla = "24x7"
}
