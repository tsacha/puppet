object Service "procs" {
  import "generic-service"

  host_name = "<%= @hostname %>"
  check_command = "by_ssh"
  vars.by_ssh_options = "StrictHostKeyChecking=no"
  vars.sla = "24x7"
  
  vars.by_ssh_command = PluginDir + "/check_procs -w 400 -c 800"
}