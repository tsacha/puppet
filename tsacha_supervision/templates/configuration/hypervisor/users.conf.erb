object Service "users" {
  import "generic-service"

  host_name = "<%= @hostname %>"
  check_command = "by_ssh"
  vars.by_ssh_options = "StrictHostKeyChecking=no"
  vars.sla = "24x7"
  
  vars.by_ssh_command = PluginDir + "/check_users -w 20 -c 50"
}